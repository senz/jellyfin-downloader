---
version: 2
updates:
  - package-ecosystem: "devcontainer"
    # Look for a `devcontainer.json` in the `root` directory
    directory: "/"
    schedule:
      interval: "monthly"
  
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "monthly"
